# WebSocket URL for the voice agent server (without /web - it's appended automatically)
VITE_API_URL=ws://localhost:8080

# Optional: API key for production (must match server API_KEY)
# When set, all API requests and WebSocket connections will be authenticated
# VITE_API_KEY=your-secret-api-key-here
