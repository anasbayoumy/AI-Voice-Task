PORT=8080
LOG_LEVEL=info

# Required: OpenAI Realtime API
OPENAI_API_KEY=sk-proj-...

# Test mode: Set to true to test WebSocket without calling OpenAI (saves money)
TEST_MODE=false

# Required: Twilio (for phone calls - both inbound and outbound)
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=+12313993815

# Required: Public URL for Twilio webhooks (use Ngrok URL for local dev)
# Example: https://abc123.ngrok.io (no trailing slash)
SERVER_PUBLIC_URL=https://your-ngrok-url.ngrok.io

# Optional: Override WebSocket URL (defaults to SERVER_PUBLIC_URL/phone)
# TWILIO_WS_URL=wss://your-domain.com/phone

# Security: API key for outbound API and WebSocket auth (optional - when unset, auth is disabled)
# Generate with: openssl rand -base64 32
# API_KEY=your-secret-api-key-here

# Status callback validation: true = block invalid signatures, false = warn only (default)
# STATUS_CALLBACK_STRICT=false
