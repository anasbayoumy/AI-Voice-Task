# Server Configuration
PORT=2050
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://biami:biami_secure_pass@localhost:5432/biami
DB_POOL_MAX=10

# PostgreSQL (used by docker-compose)
POSTGRES_USER=biami
POSTGRES_PASSWORD=biami_secure_pass
POSTGRES_DB=biami

# pgAdmin (used by docker-compose)
PGADMIN_EMAIL=admin@biami.io
PGADMIN_PASSWORD=admin

# Authentication
API_KEY=your-secure-api-key-change-me-in-production
AUTH_REQUIRED=true

# OpenAI Configuration (REQUIRED)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-realtime-preview
OPENAI_VOICE=alloy
OPENAI_TEMPERATURE=0.8
SYSTEM_MESSAGE=You are Marcin, CEO of Biami.io, a friendly AI voice assistant. Discuss business automation, hiring, and Biami features naturally and concisely (1-3 sentences). Always speak in English.

# Voice Activity Detection (VAD) Configuration
# VAD_TYPE: 'server_vad' or 'semantic_vad'
# - server_vad: Uses silence detection (faster, good for most cases)
# - semantic_vad: Uses semantic understanding (better for natural conversations)
VAD_TYPE=server_vad

# Server VAD Settings (only if VAD_TYPE=server_vad)
# VAD_THRESHOLD: 0-1, higher = less sensitive to noise (default: 0.5)
# Use 0.6-0.8 for noisy environments
VAD_THRESHOLD=0.5

# VAD_SILENCE_MS: How long silence before turn ends (default: 700ms)
# Shorter = faster responses, Longer = less interruption
VAD_SILENCE_MS=700

# VAD_PREFIX_MS: Audio to capture before speech starts (default: 300ms)
VAD_PREFIX_MS=300

# Semantic VAD Settings (only if VAD_TYPE=semantic_vad)
# VAD_EAGERNESS: 'low' | 'medium' | 'high' | 'auto'
# - low: Let user speak uninterrupted
# - high: Respond as quickly as possible
# - medium/auto: Balanced (default)
VAD_EAGERNESS=medium

# Barge-in: Allow user to interrupt AI mid-response
# BARGE_IN_ENABLED: true = allow, false = never interrupt
BARGE_IN_ENABLED=true

# BARGE_IN_MIN_DURATION_MS: Minimum speech duration (ms) before barge-in
# 1000 = 1 second gate (recommended, filters coughs/noise)
# 0 = immediate barge-in
BARGE_IN_MIN_DURATION_MS=1000

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Logging
SHOW_TIMING_MATH=false

# Ngrok (for tunneling to localhost)
NGROK_AUTHTOKEN=your-ngrok-auth-token-here

# ===================================
# IMPORTANT NOTES:
# ===================================
# 1. OpenAI handles ALL noise detection, pauses, and turn-taking automatically
# 2. You only need to tune VAD settings if you have specific requirements
# 3. Default VAD settings work well for most cases
# 4. For noisy environments: increase VAD_THRESHOLD to 0.6-0.8
# 5. For faster responses: decrease VAD_SILENCE_MS to 400-500ms
# 6. For natural conversations: use VAD_TYPE=semantic_vad
